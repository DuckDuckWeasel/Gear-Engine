# Scaffold Infra Scene Flow

`com.scaffold.sceneflow` is not embedded in this project’s `Packages/manifest.json`. Authoritative documentation lives in the Scaffold repository: [com.scaffold.sceneflow](https://github.com/MgCohen/Scaffold/tree/main/Assets/Packages/com.scaffold.sceneflow) (see `README.md` there). After adding the package to `manifest.json`, Unity resolves it under `Library/PackageCache/`.
